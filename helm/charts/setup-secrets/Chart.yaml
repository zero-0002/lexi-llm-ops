apiVersion: v2
name: setup-secrets
description: Simple secrets setup for Legal Retrieval System
type: application
version: 0.1.0
appVersion: "1.0.0"

# Không cần dependencies - chỉ tạo secrets trực tiếp
