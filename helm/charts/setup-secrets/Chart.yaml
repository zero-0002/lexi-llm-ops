apiVersion: v2
name: setup-secrets
description: External Secrets configuration for Legal Retrieval System (SecretStore + ExternalSecrets only)
type: application
version: 0.1.0
appVersion: "1.0.0"

# No dependencies - External Secrets operator installed separately
